'use client';

// Este componente não é mais usado para renderizar a apresentação,
// pois a lógica foi movida para a nova rota /presentation/[funnelId].
// Ele pode ser removido ou mantido vazio para evitar quebras em importações antigas.

export function PresentationPanel() {
  return null;
}
